<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numad21su_archanaajith">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_aa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_aa_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuArchanaAjith">
        <activity android:name=".LocationActivity"></activity>
        <activity android:name=".URLLink" />
        <activity android:name=".SixButtons" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>